[workspace]

[package]
name = "stateless-validator-reth-zisk"
version = "0.1.0"
edition = "2024"

[dependencies]
# enable features
alloy-consensus = { version = "1.5", default-features = false, features = [
    "crypto-backend",
] }
alloy-primitives = { version = "1.5", default-features = false, features = [
    "map-foldhash",
    "native-keccak",
] }
revm = { version = "34.0.0", default-features = false, features = ["bn"] }

# ere
# NOTE: Using `default-features = false, features = ["inputcpy"]` mainly to disable the other
#       feature enabled by default `user-hints`, which this binary doesn't use but introduces lots
#       of dependencies.
ere-platform-zisk = { git = "https://github.com/eth-act/ere", tag = "v0.6.0", default-features = false, features = [
    "inputcpy",
] }

# local
stateless-validator-reth = { path = "../../../crates/stateless-validator-reth", features = [
    "zisk",
] }

[profile.release]
codegen-units = 1
lto = "fat"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(zisk_hints)',
    'cfg(zisk_hints_debug)',
    'cfg(target_vendor, values("zisk"))',
] }

[patch."https://github.com/paradigmxyz/stateless"]
stateless = { git = "https://github.com/jsign/stateless", rev = "226902e1c1d9f544f5681a4ae5ab6a0e0da5cfb5" }
tries = { git = "https://github.com/jsign/stateless", rev = "226902e1c1d9f544f5681a4ae5ab6a0e0da5cfb5" }
zeth-mpt = { git = "https://github.com/jsign/stateless", rev = "226902e1c1d9f544f5681a4ae5ab6a0e0da5cfb5" }
